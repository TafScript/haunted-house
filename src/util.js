export const sound = (light) => {
    if (light.on && light.condition === "smart") new Audio('/sounds/laugh.mp3').play()
    if (light.on && light.condition === "smart") setTimeout(() => alert(text), 3000)
}

export const customClass = (light) => light.on && light.condition === "smart" ? "ghastly end" : "ghastly"

export const imgPath = () => '/images/boo.png'



























































































































































































































































































































































































































var text = 'Boo: Hahahaha... You may have lit up this house for now... but this house will ALWAYS be haunted!'